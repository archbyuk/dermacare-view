import DataManager from "@/components/desktop/data-manager";

export default function DataManagementPage() {
    return (
        <div className="h-full flex items-center gap-6 border-2 border-red-500">
            <DataManager />
        </div>
    );
}
